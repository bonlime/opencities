# train config
segm_arch: segm_fpn
arch: se_resnet50
# model_params: {"decoder_merge_policy": "cat"}
model_params: {"merge_policy": "cat"} # , "num_fpn_layers": 1
optim: novograd
lr: 1e-3
workers: 8
wd: 1e-4
size: 384
bs: 32
decoder_warmup_epochs: 20
epochs: 100
# datasets: [opencities, inria]
datasets: opencities

name: 4.segm_fpn_nov_bce_dice_seresnet50_warmup
# resume: logs/3.segm_fpn_nov_hinge_seresnet50_warmup_20200213_160637/model.chpn